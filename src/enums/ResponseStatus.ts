/**
 * Enum representing different response status values.
 * @enum ResponseStatus
 */
export enum ResponseStatus {
    SUCCESS = 'success',
    ERROR = 'error',
    PENDING = 'pending',
    REJECTED = 'rejected',
    FAILED = 'failed',
}
